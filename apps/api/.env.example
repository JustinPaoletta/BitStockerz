# BitStockerz API — copy to .env and adjust (never commit .env)

# Server
PORT=4000
NODE_ENV=development

# Logging and readiness (optional defaults)
# LOG_LEVEL=info
# LOG_TO_FILE=false
# LOG_FILE_PATH=logs/api.log
# READINESS_TIMEOUT_MS=1500

# Optional MySQL (omit for in-memory seed mode)
# Local Docker: ./scripts/docker-mysql.sh start
# DATABASE_URL=mysql://bitstockerz:devpassword@localhost:3306/bitstockerz

# Jobs & ingestion (Sprint 1.3)
JOB_TIMEOUT_MS=30000
# Set false during manual testing to disable hourly background imports
INGESTION_SCHEDULER_ENABLED=false
JOBS_SYSTEM_USER_ID=00000000-0000-4000-8000-000000000001

# Market data health & observability (Sprint 1.4)
# MARKET_DATA_HEALTH_URL=http://localhost:4000/api/market-data/health
MARKET_DATA_STALE_EQUITY_DAILY_MS=172800000
MARKET_DATA_STALE_CRYPTO_DAILY_MS=129600000
MARKET_DATA_STALE_CRYPTO_HOURLY_MS=7200000
METRICS_ENABLED=true

# In-process TTL cache + provider guardrails (Sprint 7.1)
CACHE_ENABLED=true
CACHE_CANDLES_TTL_MS=60000
CACHE_SYMBOLS_TTL_MS=60000
CACHE_MAX_ENTRIES=500
MARKET_DATA_LIVE_ENABLED=false
MARKET_DATA_CIRCUIT_FAILURES=3
MARKET_DATA_CIRCUIT_COOLDOWN_MS=60000

# Auth/session/WebAuthn limits and local defaults
# AUTH_SESSION_TTL_SECONDS=43200
# AUTH_CHALLENGE_TTL_SECONDS=300
# AUTH_OAUTH_STATE_TTL_SECONDS=300
# AUTH_RATE_LIMIT_WINDOW_MS=60000
# AUTH_RATE_LIMIT_MAX_REQUESTS=30
# WEBAUTHN_RP_ID=localhost
# WEBAUTHN_RP_NAME=BitStockerz
# Local development falls back to http://localhost:4200. Set explicitly in production.
# WEBAUTHN_ALLOWED_ORIGINS=http://localhost:4200
# Browser SPA origins for CORS (comma-separated). Defaults to http://localhost:4200 outside production.
# CORS_ALLOWED_ORIGINS=http://localhost:4200

# Backtest engine limits (Sprint 3.1)
BACKTEST_TIMEOUT_MS=5000
BACKTEST_MAX_BARS=10000
BACKTEST_MAX_SERIES_CELLS=250000
BACKTEST_RATE_LIMIT_WINDOW_MS=60000
BACKTEST_RATE_LIMIT_MAX_REQUESTS=10

# Paper trading (Sprint 4; decimal strings, values shown are defaults)
PAPER_STARTING_BALANCE=100000.00
TRADING_MAX_ORDER_NOTIONAL=25000
TRADING_MAX_POSITION_PCT=25
TRADING_MIN_CASH_REMAINING=0

# Kernel AI (Milestone 6) — keep disabled unless you explicitly opt in
AI_ENABLED=false
AI_PROVIDER=stub
AI_MODEL=gpt-4.1-mini
AI_DAILY_CALL_LIMIT=20
AI_TIMEOUT_MS=15000
AI_MAX_RETRIES=1
AI_MAX_OUTPUT_TOKENS=1200
AI_MAX_CONTEXT_CHARS=12000
AI_LOG_CONTENT=false
AI_DIFF_SUGGESTIONS_ENABLED=false
# OPENAI_API_KEY=

# Auth / OAuth (Sprint 0.2 — only if testing social login)
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:4000/api/auth/oauth/google/callback
# APPLE_OAUTH_CLIENT_ID=
# APPLE_OAUTH_TEAM_ID=
# APPLE_OAUTH_KEY_ID=
# APPLE_OAUTH_PRIVATE_KEY=
# APPLE_OAUTH_REDIRECT_URI=http://localhost:4000/api/auth/oauth/apple/callback
