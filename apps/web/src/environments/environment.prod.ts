export const environment = {
  production: true,
  /**
   * Absolute API origin without trailing slash, e.g. https://api.example.com
   * CI may rewrite this value before `ng build --configuration production`.
   * Leave empty only when Vercel rewrites `/api` to the API host.
   */
  apiBaseUrl: '',
};
